//Declare 3 variables in one statement//
var firstName, last$Name, _age;

//Declare 5 legal names//
var userName, _visit, my_Name, $price, first_name;

//Display in browser//

var rules = "<h1>Rules for naming JS variables:</h1>";
console.log(rules);
document.write(rules);

var varNames = "Variable names can only contain numbers, $, and _. For example: $my_1stVariable<br>";
console.log(varNames);
document.write(varNames);

var varLetters = "Variables must begin with a letter, $ or _ . For example: $name, _name or name<br>";
console.log(varLetters);
document.write(varLetters);

var varcase = "Variable names are case sensitive<br>";
console.log(varcase);
document.write(varcase);

var keyWords = "Variable names should not be JS keywords";
console.log(keyWords);
document.write(keyWords);